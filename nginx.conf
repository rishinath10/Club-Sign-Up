server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Client-side routes (/primary, /secondary, /teacher) have no matching file
  # on disk - fall back to index.html and let App.tsx read the path.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
